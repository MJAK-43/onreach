framework:
    rate_limiter:
        login:
            policy: 'sliding_window'
            limit: 10
            interval: '15 minutes'
        login_ip:
            policy: 'sliding_window'
            limit: 30
            interval: '15 minutes'
